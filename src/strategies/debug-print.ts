import { Print } from '../print-interface'

export class DebugPrint implements Print {
  /**
   * Prints a message in the developer tools console.
   *
   * @param message.
   *
   * @returns void.
   */
  print(_message: string): void {

  }
}